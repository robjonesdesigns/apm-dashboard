export const meta = () => [{ title: 'Work Order Management — APM' }]

export default function WorkOrderManagementRoute() {
  return (
    <div className="card">
      <h1 className="type-heading">Work Order Management</h1>
      <p className="type-body" style={{ marginTop: 'var(--spacing-16)', color: 'var(--color-text-secondary)' }}>
        Screen spec pending. See VECTOR.md "Screen Architecture" section.
      </p>
    </div>
  )
}
