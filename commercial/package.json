{
  "name": "apm-commercial",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "APM commercial v2 — relational data engine + physics-based sensor simulator",
  "scripts": {
    "seed": "node scripts/seed.mjs",
    "seed:reset": "node scripts/seed.mjs --reset"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "postgres": "^3.4.5"
  },
  "engines": {
    "node": ">=20"
  }
}
